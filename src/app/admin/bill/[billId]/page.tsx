import { Metadata } from "next";

const BillByIdPage = () => {
  return <div>BillByIdPage</div>;
};

export default BillByIdPage;
export const dynamic = "force-dynamic";
export const metadata: Metadata = {
  title: "จัดการบิล | Bill",
  description: "จัดการบิล",
};
